# RenderFlow

design tool — React + Vite 实验工作台（图像风格 / AI 线稿等）。

## 开发

```bash
npm install
npm run dev
```

## 环境变量

本地使用 Gemini 时，在项目根目录创建 `.env`（勿提交）：

```env
VITE_GEMINI_API_KEY=你的密钥
```

## 构建

```bash
npm run build
npm run preview
```
